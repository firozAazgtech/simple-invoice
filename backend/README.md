# SimpleInvoice — Backend

NestJS + TypeORM + PostgreSQL REST API.

See the [root README](../README.md) for setup instructions, reviewer credentials, architecture notes, and testing commands. Swagger docs are served at `/api/docs` once the server is running.
